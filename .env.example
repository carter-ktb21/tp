# Twilight Princess Build Configuration
# Copy this file to .env and update the values

# Path to the vanilla Twilight Princess ISO
# If not set or file doesn't exist, you'll be prompted to select one
VANILLA_ISO=orig\GZ2E01\baserom.iso

# Path where the modified ISO will be output
# Default: output_iso\modified.iso
OUTPUT_ISO=output_iso\modified.iso

# Path to Dolphin Emulator executable
# Update this to your Dolphin installation path
DOLPHIN_PATH=C:\Program Files\Dolphin-x64\Dolphin.exe

# Path to Dolphin Maps folder for symbol maps (optional)
# If not set, defaults to %APPDATA%\Dolphin Emulator\Maps (Windows)
# or ~/.var/app/org.DolphinEmu.dolphin-emu/data/dolphin-emu/Maps (Linux)
DOLPHIN_MAPS_PATH=

# Launch Dolphin after successful build (true/false)
# Set to false if you only want to build without launching
LAUNCH=true

# Dual boot mode - launch both vanilla and modified ISOs (true/false)
# Requires LAUNCH=true to work
# Opens vanilla ISO in one Dolphin instance and modified ISO in another
DUAL_BOOT=false

# Development mode (true/false)
# Enables debug features like heap display, console output, etc.
# From src/m_Do/m_Do_main.cpp
DEVELOPMENT_MODE=true
